---
import { PROCESS_STEPS } from '../../config/site';
---

<section id="proceso" class="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 border-t border-[var(--border-subtle)] relative">
  <div class="text-center max-w-3xl mx-auto mb-16 space-y-4">
    <div class="inline-flex items-center space-x-2 px-3 py-1 rounded-full bg-[var(--bg-surface-elevated)] border border-cyan-500/20 text-cyan-500 font-mono text-xs">
      <span>METODOLOGÍA & FLUJO TÉCNICO</span>
    </div>

    <h2 class="text-3xl sm:text-4xl font-extrabold text-[var(--text-primary)] tracking-tight">
      Así trabajamos
    </h2>

    <p class="text-[var(--text-secondary)] text-sm sm:text-base leading-relaxed">
      Un proceso transparente de 5 etapas para transformar una idea en un sistema en producción sin sorpresas ni demoras innecesarias.
    </p>
  </div>

  <!-- Connecting Visual Circuit Line Container -->
  <div class="relative">
    <!-- Desktop Connecting Horizontal Line -->
    <div class="hidden lg:block absolute top-[52px] left-[5%] right-[5%] h-[2px] bg-gradient-to-r from-cyan-500/10 via-cyan-400/40 to-cyan-500/10 z-0"></div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6 relative z-10">
      {PROCESS_STEPS.map((step) => (
        <div class="relative group rounded-2xl p-6 border border-[var(--border-subtle)] bg-[var(--bg-card)] hover:bg-[var(--bg-surface-elevated)] hover:border-cyan-500/40 transition-all duration-300 flex flex-col justify-between shadow-lg">
          <!-- Step indicator badge -->
          <div class="flex items-center justify-between mb-6">
            <div class="w-12 h-12 rounded-xl bg-[var(--bg-surface-elevated)] border border-cyan-500/30 text-cyan-500 font-mono text-base font-bold flex items-center justify-center group-hover:scale-110 group-hover:border-cyan-400 group-hover:shadow-[0_0_20px_rgba(6,182,212,0.3)] transition-all">
              {step.step}
            </div>
            <span class="text-[10px] font-mono text-[var(--text-muted)]">{step.code}</span>
          </div>

          <div>
            <h3 class="text-base font-bold text-[var(--text-primary)] mb-2 group-hover:text-cyan-500 transition-colors">
              {step.title}
            </h3>

            <p class="text-[var(--text-secondary)] text-xs sm:text-sm leading-relaxed">
              {step.description}
            </p>
          </div>

          <!-- Subtle bottom status -->
          <div class="mt-6 pt-3 border-t border-[var(--border-subtle)] flex items-center justify-between text-[10px] font-mono text-[var(--text-muted)]">
            <span>FASE 0{step.step}</span>
            <span class="text-cyan-500 font-semibold">VERIFICADO</span>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
